<tr id="item-<%= item.id %>">
  <td><%= item.name %> -- <%= link_to "edit", edit_item_path(item) %></td>
  <td><%= distance_of_time_in_words(Time.now, item.created_at + 7.days) %></td>

  <td><%= link_to "", item, method: :delete, remote: true, class: 'glyphicon glyphicon-ok' %></td>
</tr>
